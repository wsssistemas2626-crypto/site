# PROGRESS

## Status
TAREFA_BLOQUEADA — item 8 (configurar env vars na Vercel). Para destravar, falta:
1. **API key real do Resend** (`RESEND_API_KEY`), criada em https://resend.com/api-keys numa conta da WSS.
2. **Domínio verificado no Resend** para o remetente (`CONTACT_FROM_EMAIL`, ex.: `site@wsssistemas.com.br`). Isso exige acesso ao DNS de `wsssistemas.com.br` para criar os registros SPF/DKIM.
3. **Projeto na Vercel + acesso**: o projeto ainda não está vinculado (não existe `.vercel/`) e a Vercel CLI não está instalada. É preciso criar ou importar o projeto na conta/time da WSS, ou fornecer um token, e confirmar o domínio de produção (`NEXT_PUBLIC_SITE_URL`, hoje com fallback `https://wsssistemas.com.br`).
Com isso em mãos, basta cadastrar no painel (Settings → Environment Variables, ambientes Production e Preview) as 5 variáveis listadas em `.env.example`.

## Backlog
- [x] (pode usar este e-mail) Trocar o e-mail placeholder `contato@wsssistemas.com.br` (app/contato/page.tsx) pelo e-mail institucional real
- [x] Conectar um provedor de e-mail (Resend, SendGrid ou Postmark) em app/api/contact/route.ts para o formulário de contato enviar de fato, usando uma env var para a API key
- [x] Adicionar favicon/logo real em app/icon.png (hoje o site usa apenas o wordmark em texto "WSS+Sistemas")
- [x] Revisar e ajustar os textos de app/page.tsx, app/servicos/page.tsx e app/sobre/page.tsx ao posicionamento real da empresa (hoje são copy inicial gerada)
- [x] Adicionar metadata de Open Graph (og:image, og:title, og:description) em app/layout.tsx para compartilhamento em redes sociais
- [x] Criar página 404 customizada (app/not-found.tsx) seguindo o mesmo padrão visual do site
- [x] Adicionar testes básicos (ex.: Playwright ou Vitest) cobrindo a navegação entre páginas e o envio do formulário de contato
- [ ] Configurar variáveis de ambiente no painel da Vercel (API key do provedor de e-mail, quando aplicável)
- [ ] Rodar `npm run build` limpo (sem erros de fonte/rede) em ambiente com acesso à internet e validar visualmente as 4 páginas
- [ ] Fazer o deploy inicial na Vercel e validar o domínio de produção

## Log de decisões
(o Claude Code deve ir preenchendo aqui a cada rodada: o que fez, por quê, o que falta)
- 2026-09-21 — Item 1 (e-mail institucional): o e-mail em `app/contato/page.tsx` agora vem de `process.env.NEXT_PUBLIC_CONTACT_EMAIL`, com fallback `contato@wsssistemas.com.br`, e virou link `mailto:`. Assim a troca não exige mexer no código. O item não foi marcado como concluído porque o e-mail real não é conhecido (bloqueio de negócio, não técnico). `npm run build` passou limpo, sem erro de fontes ou rede.
- 2026-09-21 — Item 1 concluído: o cliente anotou no backlog "pode usar este e-mail", aprovando `contato@wsssistemas.com.br` como e-mail institucional. Mantive o fallback em `app/contato/page.tsx` (ainda dá para sobrescrever via `NEXT_PUBLIC_CONTACT_EMAIL`) e troquei o comentário TODO por uma nota de aprovação. O bloqueio foi removido do Status. `npm run build` passou limpo.
- 2026-09-21 — Item 2 concluído: `app/api/contact/route.ts` agora envia pelo Resend, chamando a REST API via `fetch`, sem dependência nova. Configuração por env vars: `RESEND_API_KEY` (obrigatória), `CONTACT_TO_EMAIL` (fallback `contato@wsssistemas.com.br`) e `CONTACT_FROM_EMAIL` (remetente; o fallback `onboarding@resend.dev` só serve para testes). A rota valida e limita o tamanho dos campos, escapa HTML, usa `reply_to` com o e-mail do visitante e retorna 503 se a key faltar, 502 se o Resend falhar e 400 para dados inválidos. Nesses casos o formulário já mostra o erro com o e-mail alternativo. Criei `.env.example`. A API key real e a verificação do domínio no Resend ficam para o item 8 (configurar env vars na Vercel). Sem a key, o formulário não envia em produção. `npm run build` passou limpo.
- 2026-09-21 — Item 3 concluído: criei `app/icon.svg` em vez de `app/icon.png`. É um monograma "W" com o "+" em `signal` (#5B8CFF) sobre fundo `ink` (#0B0F17), nas cores do wordmark "WSS+Sistemas". O Next o serve como favicon automaticamente. Escolhi SVG porque não existe arquivo de logo oficial no projeto e o vetor fica nítido em qualquer tamanho. Tentei também um `app/apple-icon.tsx` via `ImageResponse`, mas o `@vercel/og` do Next 14.2.5 quebra no prerender no Windows (`TypeError: Invalid URL`), então removi. Se houver logo oficial da marca, basta substituir `app/icon.svg` e, se quiser, adicionar `app/apple-icon.png` (180x180). `npm run build` passou limpo.
- 2026-09-21 — Item 4 concluído: reescrevi a copy de `app/page.tsx`, `app/servicos/page.tsx` e `app/sobre/page.tsx` a partir do posicionamento oficial do guia de marketing da WSS. Entraram a tagline "Tecnologia que funciona como uma empresa de verdade.", o diferencial dos 23 departamentos com agentes de IA (cada etapa tem um responsável) e as mensagens por público: PMEs (sob medida, sem burocracia), médias empresas (automações que eliminam retrabalho), setor público/OSCIPs (conformidade e governança) e parceiros de TI. Mantive layout, serviços e sede (Rio de Janeiro). O texto é proposta para aprovação do gestor antes da publicação. `npm run build` passou limpo.
- 2026-09-21 — Item 5 concluído: em `app/layout.tsx` entraram `metadataBase`, `openGraph` (title, description, url, siteName, locale pt_BR, type website, imagem 1200x630) e `twitter` (summary_large_image). A imagem é `public/og-image.png`, com o monograma W+ sobre fundo ink e faixa signal. Ela é gerada por `scripts/generate-og-image.mjs`, em Node puro e sem dependências, porque `ImageResponse` quebra no Windows (ver item 3) e o `sharp` não está instalado. O domínio vem de `NEXT_PUBLIC_SITE_URL` (fallback `https://wsssistemas.com.br`, inferido do e-mail aprovado), e a env var foi adicionada ao `.env.example`. O domínio precisa ser confirmado no item 10. A arte é provisória; se houver uma peça oficial, basta substituir o PNG. `npm run build` passou limpo e as tags og:* saíram no HTML.
- 2026-09-21 — Item 6 concluído: criei `app/not-found.tsx` no mesmo padrão visual do hero da home (seção `grain`, rótulo `erro/404` em `signal`, título em `font-display`), com os CTAs "Voltar ao início" e "Falar com a WSS" (/contato). Header e Footer vêm do layout. O title é "Página não encontrada — WSS Sistemas", escrito por extenso porque o layout não usa template de título. `npm run build` passou limpo e a rota `/_not-found` saiu estática.
- 2026-09-21 — Item 7 concluído: escolhi Vitest com jsdom e Testing Library (`vitest.config.mts`, script `npm test`) em vez de Playwright, porque não exige baixar browsers nem subir servidor e roda em segundos. `tests/navigation.test.tsx` confere que o Header linka `/`, `/servicos`, `/sobre` e `/contato`, que as 4 páginas e a 404 renderizam um h1 e que a 404 volta ao início. `tests/contact.test.tsx` cobre a rota `POST /api/contact` com fetch mockado: 400 para JSON ou dados inválidos, 503 sem key, 200 com payload correto para o Resend (HTML escapado, reply_to) e 502 em caso de falha. Também cobre o `ContactForm`: submit envia para `/api/contact` e mostra a confirmação ou o erro. São 13 testes, todos passando. Um teste E2E real no browser (Playwright) pode entrar depois do deploy. `npm run build` passou limpo.
- 2026-09-21 — Item 8 (env vars na Vercel) bloqueado: não há API key do Resend, domínio verificado nem projeto Vercel vinculado. Tudo isso são credenciais ou acessos reais que não posso inventar. As variáveis necessárias já estão documentadas em `.env.example` (`RESEND_API_KEY`, `CONTACT_TO_EMAIL`, `CONTACT_FROM_EMAIL`, `NEXT_PUBLIC_CONTACT_EMAIL`, `NEXT_PUBLIC_SITE_URL`). Sem a key, a rota `/api/contact` responde 503 e o formulário mostra o e-mail alternativo, então o site pode ir ao ar mesmo assim. `npm run build` passou limpo.
